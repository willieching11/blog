<%= bootstrap_form_for [@article, @article.comments.new] do |f| %>
	<body class="text-success"><%= f.alert_message "Please fix the errors below."%>
	<%= f.text_field :commenter %>
	<%= f.text_area :context %>
	<%= f.primary %></body>
<% end %>