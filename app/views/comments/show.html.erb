<% title "Comment for "+ @article.title %>

<%= render @comment %>

<%= link_to "Edit", edit_article_comment_path(@article,@comment), class: "btn btn-primary" %>
<%= link_to "Delete", article_comment_path(@article,@comment), method: :delete, data: { confirm: "Are you sure?"}, class: "btn btn-danger" %>
<br>

<%= link_to "Back", article_path(@article) %>